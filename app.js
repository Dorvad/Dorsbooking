/*
 * app.js
 * Client-side entry point for Dorsbooking.
 *
 * Responsibilities (to be implemented):
 *   - Fetch available time slots from /api/availability
 *   - Render the public booking form into #booking-root
 *   - Handle manager authentication and show/hide #manage
 *   - Submit booking requests to /api/book
 *   - Display confirmation or error feedback to the user
 *
 * No framework — plain DOM APIs only.
 */

document.addEventListener('DOMContentLoaded', () => {
  // TODO: initialise public booking view
  // TODO: check auth state and initialise manager view if authenticated
});
